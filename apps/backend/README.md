# Backend Services

This folder contains the backend services for BiteBack.

## Structure
- `services/` - Node.js and Python services
- `data/` - RAG knowledge graph, case databases
- `controllers/` - API endpoints and request handling
- `models/` - Database models and schemas
- `utils/` - Helper functions and middleware

This is the initial placeholder to create the `backend` folder inside `apps`.